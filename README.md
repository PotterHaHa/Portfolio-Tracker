# Harry Portfolio Tracker

個人台股持股追蹤工具，純前端 HTML，不需要伺服器。

## 功能
- 持股總覽（市值、損益、殖利率）
- 自動抓取 TWSE / TPEx 盤後收盤價
- 管理持股（新增、編輯、刪除）
- 退休複利試算

## 使用方式
直接用瀏覽器開啟 `index.html` 即可，或部署至任意靜態托管服務。

## 資料儲存
所有持股資料儲存於瀏覽器 `localStorage`，不上傳至任何伺服器。
